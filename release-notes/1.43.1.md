# TPS Global Context Menu 1.43.1

Native-record identity is no longer coupled to an opaque physical filename. The stable TPS ID remains in the configured property or identity tag, while the creating workflow or user may choose a readable Markdown filename.

Public `nativeRecords` API v3 adds an optional filename at creation and an atomic rename operation. GCM sanitizes requested names, keeps records in the configured root/layout, resolves collisions with deterministic numeric suffixes, updates links through Obsidian, and preserves stable identity. Manual native-record renames now persist; generic note auto-naming still ignores native records.

Minimum Obsidian: 1.10.0. Existing record schemas, storage profiles, and Legacy mode are unchanged.

## Validation and artifacts

- Validation: the focused native-record suite passed 17/17, the complete declared `npm test` suite passed (including its production build), the required separate `npm run build` passed, TypeScript passed, and `git diff --check` passed.
- Test-vault QA: the final stable build deployed only to the focused **Obsidian Plugin Test Vault** window. Source and runtime artifacts match byte-for-byte, and GCM's runtime-owned `data.json` and `.hotreload` hashes remained unchanged.
- `main.js`: `a4517334ce33e66853eb54062aae17e9b60be067a91d50ff469078f21d5ee1f4`
- `manifest.json`: `34d7c72a6b2cc80fef5340f7d1f723db936ab3a5407236decabcba55142ca9de`
- `styles.css`: `2cf3c1c5127f0bf34ba03c47416302408be318669e3ff1230e4b7a932b922f08`
- `styles-ui.css`: `572256adc422f81f32d853b9c73e92310cc3377ceab1e971b2253ec51f25cc16`

Known limitation: GCM does not bulk-rename existing records on load. Owning workflows can migrate their records during normal reconciliation; collisions are preserved with a numeric suffix rather than merged.
