Complete configurable record tags replace the mapped kind property at the shared writer/reader boundary. Tag segments have no fixed meaning or depth. Daily Note recognition and existing confirmed tag-value migration consume the same map. Custom fields now includes one compact record/tag selector when tag mappings are configured.

This is a backward-compatible minor release: tag mappings are opt-in, existing property mappings keep working, and upgrades do not migrate notes or settings automatically. Complete tag paths remain configurable. Conflicting record-type tags are rejected.

Validation: 1,242 declared tests and 171 supplemental tests passed. The installed native-record creation/update round trip and settings selector/full-tag display were verified. Temporary in-memory mappings were restored; the synthetic fixture was archived. TypeScript, full declared tests, the mandatory separate final build and test-vault deployment passed. The affected plugins were reloaded by manifest ID. No provider calls or production writes were part of QA.

Minimum Obsidian: 1.10.0. Health and Finances require GCM 3.6.0 when using tag mappings. Existing notes, templates, Bases and settings references must be migrated together after compatible runtimes are installed. GCM’s tag-value editor renames exact frontmatter values and matching GCM references; user-authored Base formulas and external-plugin references still require review. Physical iPhone acceptance remains outstanding.

Tested in Obsidian Plugin Test Vault and ready for the user's BRAT pull. This release is not claimed to be installed in production.

SHA-256:

- `main.js`: `caaa14066444ec73f13caf7d9175ae60551c3a0232464cac93be2d71a41466bf`
- `manifest.json`: `f43343d574cc9ad17667ec96481ce754015c3876b880636f5967d206bb28676e`
- `styles.css`: `3723772b1d98a7cf8dcbf6483c5f8f5f50a16daee79555cd267d7718e9a4ea9b`
- `styles-ui.css`: `572256adc422f81f32d853b9c73e92310cc3377ceab1e971b2253ec51f25cc16`
